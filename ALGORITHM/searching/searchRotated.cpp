//search in a sorted rotated array

#include<iostream>
using namespace std;

//method 1: find the pivot using binary search then we will get two sorted array on which we perform binary search

int findPivot(int arr[],int l,int r)
{
    if(l<=r)
    {
        int mid
    }
}
